import React from 'react';
import '../../css/banner.css';

export const Banner = () => {

    return (
        <div className="intro" id="slide">
            <h1>URL Shortener</h1>
            <p>Quickly shorten any URL!
                Enter your long url below and press the button to shorten it, it's that easy!</p>
        </div>
    )
}